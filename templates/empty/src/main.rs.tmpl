//! A generated module for {{ .ModuleType }} functions.

#![no_std]
#![no_main]

// `#[dagger::object]` goes on both halves of the object: on the struct it
// declares the state, on the impl below the functions. This one has no fields
// yet — add a `pub` one and it becomes state the engine carries from one call
// to the next.
/// A generated module for {{ .ModuleType }} functions.
#[dagger::object]
pub struct {{ .ModuleType }};

// The `///` below is the module description: `dagger functions` and
// `dagger call --help` show it. It has to sit on the `impl` block, because
// that is the item the engine is told about — the one on the struct above is
// only for `cargo doc`.
/// A generated module for {{ .ModuleType }} functions.
#[dagger::object]
impl {{ .ModuleType }} {}

#[goish::main]
fn main() {
    dagger::serve::<{{ .ModuleType }}>()
}
